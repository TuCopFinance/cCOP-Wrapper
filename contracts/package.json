{
  "name": "wrapped-ccop-contracts",
  "version": "1.0.0",
  "description": "Smart contracts for the Wrapped cCOP bridge",
  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat test"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^3.0.0",
    "dotenv": "^16.3.1",
    "hardhat": "^2.22.2",
    "typescript": "^5.4.3"
  }
}